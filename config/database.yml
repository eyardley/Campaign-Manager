default: &default
  adapter: postgresql
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: campaign_manager_development_database
  username: postgres
  password: admin
  host: localhost
  port: 5432 # Default PostgreSQL port

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: campaign_manager_test_database
  username: postgres
  password: admin
  host: localhost
  port: 5432
# Store production database in the storage/ directory, which by default
# is mounted as a persistent Docker volume in config/deploy.yml.
#production:
#  <<: *default
#  database: campaign_manager_database # Replace with your production database name
#  username: postgres
#  password: admin
#  host: your_production_database_host # Replace with your production database host
#  port: 5432
