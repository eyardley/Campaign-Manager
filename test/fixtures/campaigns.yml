# Campaign one: game master = user one, members = [user one, user two]
# Campaign two: game master = user two, members = [user two]

one:
  name: Campaign One
  game_master: one

two:
  name: Campaign Two
  game_master: two
